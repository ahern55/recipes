<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="recipes.Views.RecipeDetailPage"
             Title="{Binding Name}">
   
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Edit" Command="{Binding EditRecipeCommand}" />
        <ToolbarItem Text="Delete" Command="{Binding DeleteRecipeCommand}" />
    </ContentPage.ToolbarItems>


    <StackLayout Spacing="20" Padding="15">
        <!-- Grid with two columns and one row -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Text="{Binding PrepareTime, StringFormat='Prep Time: {0:0} minutes'}" FontSize="Medium" Grid.Column="0" XAlign="Center"/>
            <Label Text="{Binding CookTime, StringFormat='Cook Time: {0:0} minutes'}" FontSize="Medium" Grid.Column="1" XAlign="Center"/>
        </Grid>
        
        <Label Text="Description:" FontSize="Medium" />
        <Label Text="{Binding Description}" FontSize="Small"/>
    </StackLayout>
    
</ContentPage>